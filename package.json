{
  "name": "svelteflare",
  "version": "0.1.1",
  "private": true,
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build:development",
    "preview": "turbo run preview",
    "lint": "turbo run lint",
    "format": "oxfmt",
    "format:check": "oxfmt --check",
    "check": "turbo run check"
  },
  "devDependencies": {
    "oxfmt": "0.70.0",
    "oxlint": "1.85.0",
    "turbo": "2.11.3"
  },
  "packageManager": "pnpm@12.6.0",
  "engines": {
    "node": ">=24"
  }
}
